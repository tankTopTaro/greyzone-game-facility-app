const express = require('express');
const router = express.Router(); 
const rfidController = require('../controllers/rfidControllers')

router.get('/game-room/:gra_id', );
router.get('/booth/:booth_id', );

module.exports = router;